[workspace]
resolver = "2"
members = [
  "core/pingo-abi",
  "core/pingo-core",
  "core/pingo-edit",
  "core/pingo-headless",
  "core/pingo-hit",
  "core/pingo-layout",
  "core/pingo-paint",
  "core/pingo-scene",
  "core/pingo-scroll",
  "core/pingo-text",
  "probes/wasm-budget",
  "probes/wasm-cold-start",
]

[workspace.package]
edition = "2024"
license = "MIT"
repository = "https://github.com/dopejs/pingo"
rust-version = "1.96"

[profile.release]
codegen-units = 1
lto = true
opt-level = "z"
panic = "abort"
strip = "symbols"

# Test/debug artifacts otherwise dominate local disk usage. Line tables retain
# actionable backtraces while disabling the large incremental object cache.
[profile.dev]
debug = "line-tables-only"
incremental = false

[profile.test]
debug = "line-tables-only"
incremental = false
